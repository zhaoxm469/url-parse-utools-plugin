---
description: git提交规范
globs: *
alwaysApply: false
---
# Git 提交信息规范

为了保持项目历史清晰、一致和可维护，请遵循以下 Git 提交信息规范。

## 1. 约定式提交（Conventional Commits）

约定式提交是一种轻量级的提交信息规范，旨在使提交历史更易于阅读和自动化。

* **结构：**
    * `<类型>[可选的作用域]: <描述>`
    * `[可选的正文]`
    * `[可选的脚注]`
* **类型（Type）：**
    * `feat`：新功能（feature）。
    * `fix`：修复 bug。
    * `docs`：文档变更。
    * `style`：代码格式变更（不影响代码运行的变动，空格、格式化、分号等）。
    * `refactor`：代码重构（既不新增功能，也不修复 bug）。
    * `perf`：性能优化。
    * `test`：增加或修改测试。
    * `build`：构建流程或外部依赖变更（例如：webpack、npm）。
    * `ci`：CI 配置文件或脚本变更。
    * `chore`：其他不修改 src 或 test 的变动。
    * `revert`：回滚之前的提交。
* **可选的作用域（Scope）：**
    * 作用域用于描述本次提交影响的范围，例如：`feat(登录):`。
* **描述（Description）：**
    * 简洁明了地描述本次提交的内容。
    * 使用祈使句，例如：“添加功能”而不是“已添加功能”。
    * 不要以句号结尾。
* **可选的正文（Body）：**
    * 详细描述本次提交的变更内容、动机和实现方式。
    * 使用空行分隔段落。
* **可选的脚注（Footer）：**
    * 用于描述一些元信息，例如：关联的 issue、BREAKING CHANGES 等。
    * BREAKING CHANGES 应该在正文或者脚注的开头部分进行标明。

## 2. Angular 风格规范

Angular 团队使用的 Git 提交信息规范，也是一种非常流行的规范。

* **结构：**
    * `<类型>(<作用域>): <主题>`
    * `<空行>`
    * `<正文>`
    * `<空行>`
    * `<脚注>`
* **类型（Type）：**
    * 与约定式提交类似，但可能有一些差异。
* **作用域（Scope）：**
    * 描述本次提交影响的范围。
* **主题（Subject）：**
    * 简洁明了地描述本次提交的内容。
    * 使用祈使句，不要以句号结尾。
* **正文（Body）：**
    * 详细描述本次提交的变更内容、动机和实现方式。
* **脚注（Footer）：**
    * 用于描述一些元信息，例如：BREAKING CHANGES、关闭的 issue 等。

## 3. 通用最佳实践

* **简洁明了：** 提交信息应该简洁明了，能够清晰地表达本次提交的内容。
* **使用祈使句：** 例如：“添加功能”而不是“已添加功能”。
* **限制主题长度：** 建议主题行不超过 50 个字符。
* **使用空行分隔：** 使用空行分隔主题、正文和脚注。
* **详细描述：** 在正文中详细描述本次提交的变更内容、动机和实现方式。
* **关联 issue：** 如果本次提交关联了某个 issue，可以在脚注中进行说明。
* **BREAKING CHANGES：** 如果本次提交引入了不兼容的变更，必须在脚注中进行说明。